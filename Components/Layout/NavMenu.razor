@implements IDisposable

@inject NavigationManager NavigationManager
@inject AuthenticationStateProvider AuthenticationStateProvider


<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="flex-column">
        @* <div class="nav-item px-3"> *@
        @*     <NavLink class="nav-link" href="" Match="NavLinkMatch.All"> *@
        @*         <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home *@
        @*     </NavLink> *@
        @* </div> *@

        @* <div class="nav-item px-3"> *@
        @*     <NavLink class="nav-link" href="counter"> *@
        @*         <span class="bi bi-plus-square-fill-nav-menu" aria-hidden="true"></span> Counter *@
        @*     </NavLink> *@
        @* </div> *@

        @* <div class="nav-item px-3"> *@
        @*     <NavLink class="nav-link" href="weather"> *@
        @*         <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Weather *@
        @*     </NavLink> *@
        @* </div> *@

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="">
                <span class="bi bi-person-nav-menu" aria-hidden="true"></span> HeartPath
            </NavLink>
        </div>

        <AuthorizeView>
            <Authorized>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="users">
                        <span class="bi bi-person-nav-menu" aria-hidden="true"></span> Поиск
                    </NavLink>
                </div>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="chats">
                        <span class="bi bi-person-badge-nav-menu" aria-hidden="true"></span> Мои чаты
                    </NavLink>
                </div>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="@($"profile/{userId}")">
                        <span class="bi bi-person-nav-menu" aria-hidden="true"></span> Ваш профиль
                    </NavLink>
                </div>
                <div class="nav-item px-3">
                    <form action="Account/Logout" method="post">
                        <AntiforgeryToken />
                        <input type="hidden" name="ReturnUrl" value="@currentUrl" />
                        <button type="submit" class="nav-link">
                            <span class="bi bi-arrow-bar-left-nav-menu" aria-hidden="true"></span> Выйти из профиля
                        </button>
                    </form>
                </div>
            </Authorized>
            <NotAuthorized>
            </NotAuthorized>
        </AuthorizeView>
    </nav>
</div>

@code {
    private string? currentUrl;

    private string? userId;


    protected override async Task OnInitializedAsync()
    {
        currentUrl = NavigationManager.ToBaseRelativePath(NavigationManager.Uri);
        NavigationManager.LocationChanged += OnLocationChanged;

        var authState = await AuthenticationStateProvider.GetAuthenticationStateAsync();
        var user = authState.User;

        if (user.Identity?.IsAuthenticated == true)
        {
            userId = user.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value;
        }
    }


    private void OnLocationChanged(object? sender, LocationChangedEventArgs e)
    {
        currentUrl = NavigationManager.ToBaseRelativePath(e.Location);
        StateHasChanged();
    }

    public void Dispose()
    {
        NavigationManager.LocationChanged -= OnLocationChanged;
    }
}

<style>
    .nav-scrollable {
        background: linear-gradient(135deg, #e63b59, #e090a0); /* темнее, чем #ff4d6d и #f5b3c1 */
        min-height: 100vh;
        padding-top: 1rem;
        color: white;
        overflow-y: auto;
    }

        .nav-scrollable .nav-link {
            color: white;
            font-weight: 500;
            padding: 0.5rem 1rem;
            display: flex;
            align-items: center;
        }

            .nav-scrollable .nav-link:hover,
            .nav-scrollable .nav-link.active {
                text-decoration: none;
                opacity: 0.85;
            }

        .nav-scrollable .bi {
            margin-right: 0.5rem; /* отступ для иконок */
        }

    /* Если нужно адаптировать чекбокс для мобильного меню */
    .navbar-toggler {
        display: none; /* пока оставляем скрытым, кастомная логика */
    }
    
    .nav-link {
        display: inline-block;
        padding: 0.3rem 0.6rem;
        margin: 0.2rem 0;
        border: 2px solid white; /* белая рамка */
        border-radius: 6px; /* скругление углов */
        background-color: white; /* белый фон */
        color: white !important; /* чёрный текст */
        text-decoration: none;
        transition: all 0.2s ease;
    }

        .nav-link:hover {
            background-color: #f0f0f0; /* слегка серый при наведении */
            border-color: #ccc;
        }
</style>

